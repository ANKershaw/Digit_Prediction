apiVersion: apps/v1
kind: Deployment
metadata:
  name: digit-classifier
  labels:
    app: digit-classifier
spec:
  replicas: 2
  selector:
    matchLabels:
      app: digit-classifier
  template:
    metadata:
      labels:
        app: digit-classifier
    spec:
      containers:
      - name: digit-classifier
        image: digit-classifier:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5