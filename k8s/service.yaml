apiVersion: v1
kind: Service
metadata:
  name: digit-classifier
spec:
  type: NodePort
  selector:
    app: digit-classifier
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080